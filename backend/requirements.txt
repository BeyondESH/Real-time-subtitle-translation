# Audio capture
soundcard>=0.4.5
numpy>=1.24.0
soxr>=0.3

# Per-process audio capture (Windows 10 2004+)
comtypes>=1.4.16
pycaw>=20251023
psutil>=7.2.2

# ASR - faster-whisper (>=1.0 内置 Silero VAD)
faster-whisper>=1.0

# Translation - llama.cpp sidecar（llama-server 二进制随包分发，模型按需下载）
httpx>=0.27

# WebSocket
websockets>=12.0

# Configuration
pyyaml>=6.0

# Tests
pytest>=8.0
pytest-asyncio>=0.23
